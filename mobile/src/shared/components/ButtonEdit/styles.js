/* @flow */

import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  editButton: {
    width: 20,
    height: 20,
    top: 15,
    right: 10,
    position: 'absolute',
    zIndex: 5
  }
});

export default styles
